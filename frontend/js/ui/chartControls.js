/**
 * chartControls.js
 * Handles interactions for the Master Chart section, such as fullscreen toggle.
 * Fullscreen is now handled exclusively by KeyboardNav (native Fullscreen API).
 */

export const ChartControls = {
    init() {
        // Fullscreen toggle is managed by KeyboardNav._setupFullscreenButton()
        // using the native Fullscreen API. No duplicate handler needed here.
    }
};
