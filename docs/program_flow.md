# Application flow diagrams

The **maintained** startup, event-handling, and formula diagrams live next to the application source:

**[frontend/docs/program_flow.md](../frontend/docs/program_flow.md)**

That file is updated with the current `main.js` sequence (e.g. `SettingsPanel`, `CalorieBudgetChip`, `LOAD_SETTINGS`, lazy renderers, and the processing pipeline).

This copy in the repository root exists so older bookmarks to `docs/program_flow.md` still resolve. Please edit the canonical file under `frontend/docs/` only.
